# CC-03 测试缺陷整改记录文档

> **用途**：记录单模块测试缺陷（DEF-A-01/DEF-A-02/DEF-V-01）与 M-P/M-C 补测的**整改内容、原因分析、处理过程和最终结果**。
> **填写者**：整改子任务（执行《CC-03 测试缺陷整改说明》后填写）
> **整改基线**：`59ef3b6` · **整改完成基线**：________（填写 commit）
> **版本**：v1.0 · 2026-08-11

---

## 0. 整改概况（完成后填写）

| 项 | 值 |
|----|-----|
| 缺陷总数 | 3（DEF-A-01 / DEF-A-02 / DEF-V-01） |
| 已整改 | ____ / 3 |
| M-P 测试文件 | 已补 / 未补（`persona/file-persona-provider-test.ts`，N 用例） |
| M-C 测试文件 | 已补 / 未补（`config/loader-test.ts`，N 用例） |
| 全量回归 | 后端 ____ 文件 / 前端 ____ 脚本，全部 PASS / 有 FAIL |
| tsc（根 / client） | 通过 / 有错 |
| 前端 build | 通过 / 失败 |
| 整改 commit | ________ |

**总结论**：____

---

## 1. 缺陷整改明细

### 1.1 DEF-A-01：orchestrator 降级测试期望过时（M-A-TC-009/012）
- **原因分析**：____（CC-01 L9 脱敏后，测试仍断言具体错误文本）
- **整改内容**：____（更新断言为通用提示"大脑开小差了"）
- **处理过程**：____
- **验证结果**：____（orchestrator-degradation-test 12/12）
- **状态**：✅ 已验证关闭 / ❌ 未完成（原因：____）

### 1.2 DEF-A-02：ws-smoke personasDir 路径校验拦截（M-A-TC-014/015/016）
- **原因分析**：____（H6 路径校验 + smoke 测试环境 personasDir 不匹配）
- **整改内容**：____（修正 smoke 配置指向真实/临时人设目录）
- **处理过程**：____
- **验证结果**：____（ws-smoke-test 4/4）
- **状态**：✅ / ❌（原因：____）

### 1.3 DEF-V-01：gateway 二进制音频帧不直通（M-V-TC-042）
- **原因分析**：____（前端 base64 JSON，二进制帧为设计边界）
- **整改内容**：____（删除用例并注明 / gateway 加 Buffer 分支）
- **处理过程**：____
- **验证结果**：____（gateway-unit-test 26/26）
- **状态**：✅ / ❌（原因：____）

---

## 2. 新增测试文件记录

### 2.1 persona/file-persona-provider-test.ts（M-P）
| 用例 | 类型 | 断言 | 结果 |
|------|------|------|------|
| listPersonas 返回注册表 | 正常路径 | 全部角色 | ✅/❌ |
| getPersona 加载 card | 正常路径 | 指令含 card 内容 | ✅/❌ |
| buildInstructions 拼接 | 正常路径 | 含身份/性格/风格 | ✅/❌ |
| 路径越界防护 | 异常输入 | ../ 拒绝 | ✅/❌ |
| 缺失文件兜底 | 异常输入 | ok:false/默认 | ✅/❌ |
| 毫秒级读取 | 性能 | <100ms | ✅/❌ |

### 2.2 config/loader-test.ts（M-C）
| 用例 | 类型 | 断言 | 结果 |
|------|------|------|------|
| 文件优先 | 正常路径 | 采用文件值 | ✅/❌ |
| 环境变量兜底 | 边界条件 | 文件缺失用 env | ✅/❌ |
| 缺失 Key 处理 | 异常输入 | undefined/默认 | ✅/❌ |
| maskKey 脱敏 | 正常路径 | 掩码显示 | ✅/❌ |
| port 默认值 | 边界条件 | 未配置 → 3000 | ✅/❌ |

---

## 3. 回归测试记录

| 测试项 | 命令 | 结果 |
|--------|------|------|
| 根 tsc | `npx tsc --noEmit` | ✅ / ❌ |
| client tsc | `cd client && npx tsc --noEmit` | ✅ / ❌ |
| gateway 单测 | `node --experimental-strip-types voice-shell/gateway-unit-test.ts` | ✅ / ❌ |
| dispatcher | `node --experimental-strip-types voice-shell/dispatcher-unit-test.ts` | ✅ / ❌ |
| fc | `node --experimental-strip-types voice-shell/function-calling-unit-test.ts` | ✅ / ❌ |
| vad | `node --experimental-strip-types voice-shell/vad-unit-test.ts` | ✅ / ❌ |
| transcript | `node --experimental-strip-types voice-shell/transcript-unit-test.ts` | ✅ / ❌ |
| emotion-matcher | `node --experimental-strip-types avatar/emotion-matcher-unit-test.ts` | ✅ / ❌ |
| qwen-fallback | `node --experimental-strip-types brain/qwen-fallback-test.ts` | ✅ / ❌ |
| ws-test | `node --experimental-strip-types app/server/ws-test.ts` | ✅ / ❌ |
| **ws-smoke** | `node --experimental-strip-types app/server/ws-smoke-test.ts` | ✅ / ❌ |
| **orchestrator-degradation** | `node --experimental-strip-types app/server/orchestrator-degradation-test.ts` | ✅ / ❌ |
| **persona（新增）** | `node --experimental-strip-types persona/file-persona-provider-test.ts` | ✅ / ❌ |
| **config（新增）** | `node --experimental-strip-types config/loader-test.ts` | ✅ / ❌ |
| client avatar | `cd client && npm run test:avatar` | ✅ / ❌ |
| client chat | `cd client && npm run test:chat` | ✅ / ❌ |
| client 其余 | `test:chat-hook / test:caption / test:waveform` | ✅ / ❌ |
| 前端 build | `cd client && npm run build` | ✅ / ❌ |

---

## 4. 最终结论与遗留事项

**整改结论**：____（3 缺陷是否全部关闭、M-P/M-C 是否补测、全量是否通过）

**遗留事项**：
1. ____
2. ____

**整改人**：____ · 日期：________ · 整改 commit：________

---

## 5. 更新记录

| 日期 | 执行者 | 更新内容 |
|------|--------|----------|
| 2026-08-11 | 架构负责人 | 文档初版创建（配套《CC-03 测试缺陷整改说明》） |

---

*CC-03 测试缺陷整改记录 v1.0 · 2026-08-11 · 配套《CC-03 测试缺陷整改说明》*
