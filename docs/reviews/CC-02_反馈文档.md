# CC-02 反馈文档（依赖与安全审计结果）

> **填写者**：Claude Code
> **任务编号**：CC-02 · 对应说明文档 `docs/reviews/CC-02_说明文档.md`
> **审计日期**：2026-08-11
> **审计范围**：根 package.json + client/package.json
> **npm 版本**：12.0.2

---

## 一、审计概况

| 项 | 内容 |
|----|------|
| 依赖总数（根 / client） | 6 / 8 |
| 运行时依赖（根） | 2（express + ws）✅ |
| 运行时依赖（client） | 2（react + react-dom）✅ |
| 最小化达标（红线 5） | ✅ 完全达标 |
| 漏洞总数（根 / client） | 0 / 2 |
| critical / high | 0 / 1（high 为 esbuild 传递依赖） |
| moderate | 0 / 1 |
| deprecated 包数 | 0 |
| 硬编码密钥发现 | 0（达标） |
| 基线问题 | 0（无已知基线问题） |

**总体结论**：依赖健康度**优秀**——依赖最小化完全达标，密钥管理合规，仅 client 开发工具链存在 2 个漏洞（esbuild/vite，仅影响开发环境），生产部署无安全风险。

---

## 二、问题清单

### 🔴 高严重度（安全漏洞 critical/high）

| # | 包名 | 版本 | 漏洞描述 | 影响版本 | 修复建议 |
|---|------|------|----------|----------|----------|
| 1 | esbuild（via vite） | 0.21.5 | GHSA-67mh-4wv8-2f99：esbuild 开发服务器允许任意网站发送请求并读取响应（CSWSH 类漏洞）。vite <=6.4.2 依赖了此版本，标记为 high | <=0.24.2 | 升级到 `esbuild@0.28.2+` + `vite@7.0.0+`；或接受风险（仅影响开发环境） |

### 🟡 中严重度（moderate 漏洞 / 配置问题）

| # | 位置 | 问题描述 | 影响范围 | 建议 |
|---|------|----------|----------|------|
| 1 | client: esbuild@0.21.5 | esbuild 自身漏洞（moderate）：开发服务器 CSWSH | 仅开发环境（`vite dev`） | 同上方 high 修复方案 |

### 🟢 低严重度 / 优化建议

| # | 位置 | 问题描述 | 影响范围 | 建议 |
|---|------|----------|----------|------|
| 1 | 根: express@4.22.2 | 有 major 版本更新可用（4.x → 5.2.1） | 无安全影响 | 4.x 稳定 LTS，当前无需升级；5.x 可在下个大版本评估 |
| 2 | npm audit | npmmirror 镜像不支持 `/security/advisories` 端点 | 审计时需手动指定 registry | 审计时加 `--registry=https://registry.npmjs.org` |

---

## 三、依赖清单分析

### 根（后端）

| 包名 | 类型 | 版本 | 用途 | 是否必要 | 备注 |
|------|------|------|------|----------|------|
| express | dependency | 4.22.2 | HTTP 服务器框架 | ✅ 必 | 有 5.x 可用但不急 |
| ws | dependency | 8.21.3 | WebSocket 服务器/客户端 | ✅ 必 | 最新版本 |
| @types/express | devDependency | 5.0.6 | Express 类型定义 | ✅ 必 | |
| @types/node | devDependency | 26.2.0 | Node.js 类型定义 | ✅ 必 | |
| @types/ws | devDependency | 8.18.1 | ws 类型定义 | ✅ 必 | |
| typescript | devDependency | 7.0.2 | TypeScript 编译器 | ✅ 必 | `tsc --noEmit` |

### client（前端）

| 包名 | 类型 | 版本 | 用途 | 是否必要 | 备注 |
|------|------|------|------|----------|------|
| react | dependency | 18.3.1 | UI 框架 | ✅ 必 | |
| react-dom | dependency | 18.3.1 | React DOM 渲染 | ✅ 必 | |
| @types/react | devDependency | 18.3.31 | React 类型定义 | ✅ 必 | |
| @types/react-dom | devDependency | 18.3.7 | ReactDOM 类型定义 | ✅ 必 | |
| @vitejs/plugin-react | devDependency | 4.7.0 | Vite React 插件 | ✅ 必 | Fast Refresh |
| esbuild | devDependency | 0.21.5 | 打包工具 | ✅ 必 | voice-test.ts 用，⚠️ 有漏洞 |
| typescript | devDependency | 5.9.3 | TypeScript 编译器 | ✅ 必 | |
| vite | devDependency | 5.4.21 | 开发服务器 + 构建 | ✅ 必 | ⚠️ 传递依赖漏洞 |

---

## 四、整改建议（优先级 + 实施方案）

| 优先级 | 关联问题 # | 整改建议 | 实施方案 | 预估工作量 |
|--------|-----------|----------|----------|-----------|
| P1 | 🔴1, 🟡1 | 修复 esbuild/vite 漏洞 | `cd client && npm install esbuild@latest vite@latest`，然后 `npm run test:voice` 验证 | 30min |
| P2 | 🟢1 | 评估 express 5.x | 下个 major 版本时评估迁移 | - |
| P3 | 🟢2 | 审计脚本配置 | 项目 `.npmrc` 中添加 `audit.registry=https://registry.npmjs.org` 或文档记录 | 5min |

---

## 五、修改后验证方式

| 关联问题 # | 验证方法 | 预期结果 |
|-----------|----------|----------|
| 🔴1, 🟡1 | `cd client && npm audit --registry=https://registry.npmjs.org` | 0 vulnerabilities |
| 🔴1, 🟡1 | `npm run test:voice` | 测试通过 |
| 全局 | `npm ls --depth=0`（根 + client） | 依赖清单干净，无 UNMET/INVALID |

---

## 六、最终结论与遗留事项

**审计结论**：✅ **通过**。依赖清单完全符合红线 5（依赖最小化），密钥管理完全合规（红线 8），仅 client 开发工具链存在 2 个漏洞（esbuild moderate + vite high，仅影响开发环境），不影响生产部署安全。

**遗留事项**：
1. **esbuild/vite 升级**（P1）：建议升级到最新版本修复漏洞，需验证 voice-test.ts 的 esbuild 打包脚本兼容性
2. **express 5.x 评估**（P2）：当前 4.x 稳定，可在下个大版本评估迁移
3. **npmmirror audit 支持**（P3）：当前 npmmirror 不支持 audit 端点，审计时需切换到官方 registry

**审计人**：Claude Code · 日期：2026-08-11
**审计报告**：`docs/reviews/dependency-audit-2026-08-11.md`

---

*CC-02 反馈文档 v1.0 · 2026-08-11 · Claude Code 生成*
