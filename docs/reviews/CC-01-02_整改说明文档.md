# CC-01/CC-02 整改说明文档（子任务执行文档）

> **执行者**：WorkBuddy（新聊天框子任务）——**只读本文件即可独立、无歧义地完成整改**，无需额外解释。
> **任务**：对 CC-01（代码审查）反馈的 48 个问题 + CC-02（依赖审计）反馈的问题进行整改。
> **前置依赖**：CC-01 审查已完成（反馈：`docs/reviews/CC-01_反馈文档.md`，报告：`docs/reviews/code-review-2026-08-11.md`）；**CC-02 审计已完成**（反馈：`docs/reviews/CC-02_反馈文档.md`，报告：`docs/reviews/dependency-audit-2026-08-11.md`）。
> **版本**：v1.1 · 2026-08-11（v1.1 新增 CC-02 整改范围）

---

## 1. 整改目标

### 1.1 总目标
消除 CC-01 代码审查发现的**全部 48 个问题**（🔴高 9 / 🟡中 18 / 🟢低 21）+ CC-02 依赖审计发现的问题（esbuild/vite 漏洞等），且**不引入新缺陷、不破坏现有功能**。

### 1.2 分优先级目标
| 优先级 | 范围 | 目标 |
|--------|------|------|
| **P0（必修）** | H1, H4, H7, H8, H13 | 超时保护 + try-catch + 死代码删除 + WS 帧类型 |
| **P1（必修）** | H2, H3, H5, H6, H9 + 相关 | config 语义 + WS 安全 + 输出计数 + 路径安全 + 前端竞态 |
| **P1（必修·CC-02）** | CC-02 🔴1, 🟡1 | esbuild/vite 漏洞修复（开发工具链） |
| **P2（应修）** | M1~M18 + CC-02 🟢1 | 状态码重构 + 路径修正 + 资源清理 + 边界保护 + express 5.x 评估 |
| **P3（尽力）** | L1~L21 + CC-02 🟢2 | 低优先级改进（时间不足可记录未改原因）+ audit 配置 |

> ⚠️ **P0+P1 必须全部完成**（含 CC-02 漏洞修复）；P2 尽量完成；P3 按时间尽力，未处理的必须记录在整改文档"遗留事项"。

---

## 2. 整改范围

### 2.1 涉及文件（必须修改/删除）

| 模块 | 文件 | 需处理问题 |
|------|------|-----------|
| app | `app/server/orchestrator.ts` | H1, M2, L7-L9 |
| app | `app/server/routes.ts` | M1, M15, L5-L6 |
| app | `app/server/ws.ts` | H3, L10-L11 |
| app | `app/server/index.ts` | M3, L1-L4 |
| brain | `brain/hermes-runner.ts` | H5, M6-M7, L12-L13 |
| brain | `brain/function-router.ts` | H4, M8, L14-L15 |
| brain | `brain/qwen-fallback.ts` | M9, L16 |
| persona | `persona/file-persona-provider.ts` | H6, M10-M12 |
| persona | `persona/hermes-persona-provider.ts` | **H7 删除**（死代码） |
| persona | `persona/default-persona-provider.ts` | **M13 删除**（死代码） |
| voice-shell | `voice-shell/gateway.ts` | H8, M17 |
| voice-shell | `voice-shell/qwen-audio-client.ts` | M16 |
| avatar | `avatar/clip-matcher.ts` | L18 |
| avatar | `avatar/emotion-matcher.ts` | M14, L19 |
| config | `config/loader.ts` | H2, M4-M5 |
| client | `client/src/hooks/useVoice.ts` | H9, L20 |
| client | `client/src/hooks/use-chat.ts` | L21 |
| client | `client/src/components/AvatarCanvas.tsx` | M18 |
| 契约 | `docs/architecture/module-contracts.md` | 补 `listPersonas()` 接口定义（遗留事项 3） |

### 2.2 CC-02 依赖审计整改范围（P1 必修）

| 问题 | 位置 | 整改方案 | 验证 |
|------|------|----------|------|
| CC-02 🔴1（high） | `client/node_modules/esbuild@0.21.5`（vite 传递依赖） | `cd client && npm install esbuild@latest vite@latest`（或 `npm audit fix --registry=https://registry.npmjs.org`） | `npm audit` 复扫 0 漏洞 |
| CC-02 🟡1（moderate） | 同上（esbuild 自身） | 随 esbuild 升级一并修复 | 同上 |
| CC-02 🟢1 | express@4.22.2（有 5.x 可用） | **仅评估不升级**（4.x 稳定 LTS），记录结论 | - |
| CC-02 🟢2 | npmmirror 不支持 audit 端点 | `.npmrc` 加 `audit.registry=https://registry.npmjs.org` 或文档记录 | audit 命令可用 |

> ⚠️ **升级 esbuild/vite 后必须验证**：`cd client && npm run test:voice`（voice-test.ts 依赖 esbuild 打包）+ `npm run build`。

### 2.3 范围外（本次不整改）
- ❌ M5-01 性能瓶颈（Hermes 冷启动 20-39s）——已决策由 ACP 常驻治理，另行任务
- ❌ 认证机制设计（H3 涉及的"无认证"）——生产部署前单独设计，本次仅做 WS 连接数限制 + Origin 校验的**基础加固**

---

## 3. 具体实施步骤（分阶段）

### 阶段 0：环境准备（5 分钟）
```bash
cd D:/其他资料/赛博女友
git log --oneline -1        # 确认基线（应为 d0dfe05 或其后）
npx tsc --noEmit            # 记录基线（应零错误）
npm test 2>/dev/null || echo "无根测试脚本，跳过"
```

### 阶段 1：读问题清单（10 分钟）
1. 通读 `docs/reviews/CC-01_反馈文档.md` 的 §二 问题清单（H1~H9, M1~M18, L1~L21）
2. 对照 `docs/reviews/code-review-2026-08-11.md` §2 看每个问题的完整描述与建议
3. 按 §2.1 表格**逐文件**定位问题行号

### 阶段 2：P0 整改（必修，先做）
按以下顺序逐项修复，**每改完一个模块跑一次 tsc**：

```bash
# ① H7/M13：删除死代码
git rm persona/hermes-persona-provider.ts persona/default-persona-provider.ts
#   检查引用：grep -rn "hermes-persona-provider\|default-persona-provider" app/ brain/ persona/ --include="*.ts"

# ② H4：function-router.ts:199 handle() 加 try-catch（包裹 runner.run，失败构造 failed 写回）

# ③ H1：orchestrator.ts:83-131 chat() 加 Promise.race 总超时（建议 60s 文本 / 90s 复杂）

# ④ H8：gateway.ts:122 JSON 消息改文本帧
#   browserWs.send(Buffer.from(JSON.stringify(obj))) → browserWs.send(JSON.stringify(obj))
```

### 阶段 3：P1 整改
```bash
# ⑤ H2：loader.ts 全部 || 改 ??（port 字段保留 ||）
# ⑥ H3：ws.ts 加 maxConnections（默认 10）+ Origin 校验（允许 localhost 任意端口）
# ⑦ H5：hermes-runner.ts stdout/stderr 分开计数（MAX_OUTPUT_BYTES 只限 stdout）
# ⑧ H6：file-persona-provider.ts buildClosingInstruction 路径加 startsWith(personasDir) 校验
# ⑨ H9：useVoice.ts connect() 加 connectingRef 防重入
```

### 阶段 4：P2 整改（按文件批量）
按 §2.1 表格逐文件处理 M1~M18（详见反馈文档对应行的建议）：

```bash
# 关键项：
#  M1: routes.ts 用自定义 Error 类决定状态码（不再字符串匹配）
#  M3: index.ts shutdown 加防重入（flag 变量）
#  M4/M5: loader.ts 用 import.meta.url 推导根目录，personasDir 不硬编码
#  M14: emotion-matcher.ts recentlyPlayedWindow 校验 NaN/Infinity（Math.max(1, Math.min(n,100)))
#  M16: qwen-audio-client.ts connect 超时后 clearInterval(reconnectTimer)
#  M17: gateway.ts onInputTranscript 并入 dispatcher 分发
#  M18: AvatarCanvas.tsx 播放 effect 用 useRef 防止 state 变化重新 load()
```

### 阶段 4.5：CC-02 依赖漏洞修复（P1 必修）
```bash
# 修复 esbuild/vite 漏洞（CC-02 🔴1 + 🟡1）
cd client && npm install esbuild@latest vite@latest
#   或：cd client && npm audit fix --registry=https://registry.npmjs.org
cd .. 

# 验证升级无破坏
cd client && npm run test:voice && npm run build && cd ..
# 复扫确认
cd client && npm audit --registry=https://registry.npmjs.org && cd ..
```

### 阶段 5：P3 整改（尽力）
按 L1~L21 逐项处理，单项简单直接改；耗时项可跳过并在整改文档记录。

### 阶段 6：全量验证（30 分钟）
```bash
# ① 类型检查
npx tsc --noEmit                                  # 必须零错误
cd client && npx tsc --noEmit && cd ..            # 前端也零错误

# ② 全量测试（回归）
node --experimental-strip-types voice-shell/gateway-unit-test.ts
node --experimental-strip-types voice-shell/dispatcher-unit-test.ts
node --experimental-strip-types voice-shell/function-calling-unit-test.ts
node --experimental-strip-types voice-shell/vad-unit-test.ts
node --experimental-strip-types voice-shell/transcript-unit-test.ts
node --experimental-strip-types avatar/emotion-matcher-unit-test.ts
node --experimental-strip-types brain/qwen-fallback-test.ts
node --experimental-strip-types app/server/ws-test.ts
cd client && npm run test:avatar && npm run test:chat && npm run test:chat-hook && cd ..
cd client && npm run test:caption && npm run test:waveform && cd ..
cd client && npm run build && cd ..               # 前端构建

# ③ 启动冒烟（可选，若环境允许）
node --experimental-strip-types app/server/index.ts &  # 后端起来后 curl /api/health
```

### 阶段 7：填整改文档（20 分钟）
按《CC-01/02 整改文档》模板逐条填写：整改内容、原因分析、处理过程、验证结果。

### 阶段 8：提交入库
```bash
git add -A
git commit -m "fix: CC-01 整改 - <概述>（P0/P1/P2/P3 完成情况）"
git push origin main
```

---

## 4. 验收标准（什么算完成）

- [ ] **P0+P1 全部完成**（H1~H9 + CC-02 漏洞修复无遗漏）
- [ ] P2 完成 M1~M18（个别无法完成需在整改文档注明原因）
- [ ] `npx tsc --noEmit`（根 + client）零错误
- [ ] 全部现有测试通过（阶段 6 列出的测试命令无 FAIL）
- [ ] 前端 `npm run build` 通过
- [ ] **CC-02 漏洞复扫 0 漏洞**（`npm audit --registry=https://registry.npmjs.org`）
- [ ] 死代码已删除（`git rm` 确认）
- [ ] **整改文档已完整填写**（每个问题有对应整改记录）
- [ ] 提交已推送 GitHub

> ⚠️ **未满足上述任一条件 = 任务未完成**，不允许提前结束。

---

## 5. 注意事项与红线

### 红线（不可违反）
1. **不引入新依赖**（package.json 依赖保持 express + ws + react，红线 5）
2. **不新增持久化**（无数据库、无文件写入业务数据，红线 1）
3. **契约优先**：涉及接口变更（如 M1 状态码、L20 WS 协议）先改 `module-contracts.md` 再改代码
4. **记忆隔离**：不动 Hermes profile、不读写密钥文件（红线 8/10）
5. **不破坏现有测试**：已通过的单测必须保持通过（回归）

### 注意
- 每个问题修复后**立即记录**到整改文档（防止遗漏）
- 若某个问题修复需要重构大段代码且风险高 → 在整改文档标注"建议单独任务处理"，不硬改
- 删除文件前确认无引用（`grep -rn` 验证）
- 改完记得**更新文档注释**（反馈文档提到 provider.ts/index.ts 注释过时问题 L3 相关）

---

## 6. 交付物清单

| # | 交付物 | 路径 |
|---|--------|------|
| 1 | 整改后的代码（修改/删除的文件） | 项目内各模块 |
| 2 | **整改文档（必填）** | `docs/reviews/CC-01-02_整改文档.md`（模板见配套文档，按 §2.1 逐条填写） |
| 3 | DEVLOG 记录 | `docs/DEVLOG.md` 最上方追加 |
| 4 | 契约同步（如涉及） | `docs/architecture/module-contracts.md` |

---

*CC-01/02 整改说明文档 v1.0 · 2026-08-11 · 配套《CC-01/02 整改文档》*
