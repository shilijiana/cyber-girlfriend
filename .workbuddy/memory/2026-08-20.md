# 2026-08-20 工作日志

## 数字人主视觉迭代：Pexels 真实背景 + 赛博朋克风人物（定稿 v2）

- **背景**：老板用 Pexels 图 `pexels-thngocbich-622164.jpg`（4096×2300 横屏，暗调书房：蓝窗+显示器+台灯+暖光灯串）当背景，人物用 `image_999192722333225.png`（HY-Image 生成赛博朋克风年轻女性：透明雨衣+黑背心+短裤，2496×3744）。
- **迭代过程**：
  - v1 竖图（人物小、比例失真）→ 老板否
  - v2 横图（4096×2300，人物 1829×2700 右侧头顶对齐、脚溢出 400px，头发压暗 0.80 渐变）→ **老板认可"效果更好"**
  - v3 整体压暗 20% → 老板否（"还是 v2 效果更好，回退 v2"）
- **定稿**：`人物模型/cyber_final_v2.jpg`（= test_cyber_v2.jpg 副本）。参数：人物 1829×2700（占背景高 117%），贴右侧 x=2237、y=0（头顶对齐）、脚溢出 400px；头发顶部压暗渐变。
- **技术要点**：rembg u2netp 抠图 1.5s（2496×3684）；Pexels 图无需去水印；人物风格=赛博朋克（与"白裙珍珠"淑女风是两套形象方向，待老板确认最终用哪套）。
- **下一步候选**：用 v2 定稿构图做视频（人物动态+此背景）或继续微调。

## 素材方向变更为横屏（项目级规范）

- **老板要求**：后续所有图片和视频均为**横屏 16:9**（不再用竖屏）。已写入 MEMORY.md。
- **新方案**：用 `cyber_final_v2.jpg`（v2 主视觉，4096×2300 横屏）作为 VideoGen 图生视频输入，缩到 1280×720 解决 body 过大问题 → 输出 1280×720 横屏视频。
- **微笑视频 v2 提示词定型**（针对变形问题优化）：嘴角"幅度轻柔克制"+面部肌肉完全放松+五官比例协调自然+表情自然不夸张。生成后自然柔和、五官无变形。
- **完整链路（横屏版）**：
  1. `cyber_final_v2.jpg` → resize 1280×720（`cyber_v2_video_input.jpg`）→ VideoGen 图生视频（5秒 720P）
  2. ffmpeg 裁水印（`crop=1280:670:0:0`）
  3. RVM mobilenetv3 抠视频 pha + chroma 合成贴 Pexels 横屏原图（4096×2300 → resize 1280×720）
  4. 输出 `smile_final_wide.mp4`（1280×720 横屏）
- **新资产**：
  - `横屏_16_9_数字人视频_..._2026-08-19T16-38-25.mp4`（带水印测试版，1280×720）
  - `smile_wide_crop.mp4`（裁水印后 1280×670）
  - `smile_final_wide.mp4`（最终成片，待出）
- **警示**：之前所有竖屏素材（白裙珍珠定妆照/视频 + 早期赛博朋克视频）都是竖屏，可能需要重新生成横屏版，等老板确认是否要重做。

## 5 段情绪视频全部完成（1080P 横屏终稿）

- **定稿主视觉**：`cyber_final_v2.jpg`（赛博朋克人物 + Pexels 书房，横屏 4096×2300）
- **表情提示词基调（V3 微笑风格）**：自然、克制、不夸张——嘴角幅度轻柔/极轻、面部肌肉放松、五官比例协调、嘴型闭合、动作轻微。经验：提示词里写明"幅度克制/不夸张/面部放松"可避免 AI 生成表情变形。
- **标准流程（已跑通 5 段）**：
  1. `cyber_video_input.jpg`（原图压缩 719×1080）→ VideoGen（5 秒 1080P，输出 1172×1764）
  2. ffmpeg 裁水印（`crop=1172:1704:0:0`）
  3. `rvm_compose_wide.py`：RVM 抠 pha → 人物层缩放（854×1242，scale=1.15）→ 贴 Pexels 横屏背景（1920×1080，x=1036 右侧、头顶对齐、脚溢出 162px）
  4. 输出 `cyber_<emotion>_1080p.mp4`
- **终稿清单**（均 1920×1080 / 5.04s / 无水印）：
  - `cyber_smile_1080p_final.mp4`（微笑 V3，老板定稿）
  - `cyber_happy_1080p.mp4`（开心）
  - `cyber_gentle_1080p.mp4`（温柔）
  - `cyber_neutral_1080p.mp4`（平静）
  - `cyber_serious_1080p.mp4`（认真）
  - `cyber_surprise_1080p.mp4`（惊讶）
- **踩坑**：并行调用 VideoGen 时**文件名时间戳冲突**（秒级精度），3 段互相覆盖 → **必须串行生成**或生成后立即改名。
- **中间产物**（可清理）：`cyber_*_crop.mp4`、`smile_wide_crop*`、`preview_*.jpg`、`check_*.jpg`、`test_*.jpg/png`、`debug_*`、`*_cutout*.png` 等（待老板确认后清理，保留原始生成 + 终稿）。

## 情绪视频接入赛博女友（测试就绪）

- **时间**：20:49 左右。
- **操作**：
  1. 6 段终稿复制到 `assets/avatars/clips/`（命名 `cyber_<emotion>_01.mp4`，微笑归 neutral 变体 `cyber_smile_01.mp4`）
  2. 更新 manifest（version 2）：`assets/avatars/manifest.json` + `avatar/manifest.json` 两处同步，6 条 clip（emotion: happy/gentle/neutral(×2: neutral+smile)/serious/surprise，durationSec 5.04）
  3. 启动服务验证：`/api/avatar/status` → `{"engine":"clip","clipCount":6}`；6 段视频 `/avatars/clips/*.mp4` 全部 HTTP 200
- **前端零改动**：`/avatars` 静态映射（app/server/index.ts）+ manifest 结构不变。
- **测试启动**：`npm run dev`（或 `node --experimental-strip-types app/server/index.ts`），浏览器访问 localhost:3000。

## 测试素材清理 + 前端启动修正

- **前端访问地址修正**：Express 后端（3000 端口）只提供 API/静态素材/WS，**前端页面在 Vite dev server 5173 端口**（`cd client && vite`，/api、/ws、/avatars 代理到 3000）。访问 `http://localhost:5173`。
- **旧占位素材清理**：`assets/avatars/` 下 14 个旧测试素材（anime_girl_1~3、girl_portrait_1~3 mp4 + cute_anime_girl_avatar_1~5、happy_girl_portrait_colorful_1~3 jpeg）移到 `assets/avatars/_archive_20260820/`（可恢复归档）。当前目录只剩 6 段新素材 + manifest.json。
- **验证**：`/api/avatar/status` → clipCount 6；视频访问 HTTP 200。
- **注意**：PowerShell Add-Type（回收站删除）被安全策略拦截，改用 mv 归档方式处理删除类操作。
