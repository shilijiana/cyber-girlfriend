# 2026-08-09 工作日志

## 赛博女友项目启动（调研阶段）
- 老板提出「赛博女友」项目：CodeBuddy SDK + S2S 端到端语音 + 数字人（表情/口型同步）+ 4 个 MCP（Work Buddy / Hermes / VSCODE / CODEX）。
- 按老板惯例先调研后动手：GitHub 搜索 + 网络调研完成。
- 关键结论：
  - S2S 云 API 首选 MiniMax Speech 2.6（<250ms，被 LiveKit/Pipecat 采用）；自托管可考虑 Qwen3-Omni / MiniCPM-o。
  - 数字人推荐 Live2D（pixi-live2d-display）+ 音频能量口型驱动；视频级（Wav2Lip/LatentSync）延迟高不选。
  - 不引入 Pipecat/LiveKit 双语言框架，保持 TS 单栈。
- 产出：`PROJECT_MEMORY.md`（项目根目录，按老板要求章节）。
- 待老板确认：S2S 选型、数字人形态，之后开始 M1 脚手架搭建（用 codebuddy-chat-web 技能）。

## 老板拍板 + 设计文档（同日追加）
- 老板确认：① S2S 用**云端 API**（Provider 可插拔，默认 GLM-Realtime，Qwen 端到端/豆包 Seeduplex 备选；MiniMax Speech 2.6 已排除为纯 TTS）② 数字人用**真人视频级口型**（MuseTalk 实时主轨 MIT + LatentSync 1.5 高质量轨 Apache-2.0 + Live2D 无 GPU 兜底，Python sidecar 独立部署）③ 先出设计文档再动手。
- 产出：`DESIGN.md`（详细设计 v0.1：架构/Provider 抽象/MuseTalk 双轨/MCP 配置模板/接口定义/数据模型/目录/环境变量/测试/里程碑/风险）。
- 下一步：等老板评审 DESIGN.md，通过后 M1 用 init-cbc-sdk-web 搭脚手架。

## 老板改需求：数字人改为素材库方案（同日追加）
- 老板反馈：真人视频级口型对电脑配置要求太高 → 改为**提前准备一批视频，数字人模仿说话，口型不要求完全对上**。
- 设计 v0.2 调整：默认数字人引擎 `clip_library`（素材库）：idle/speaking(按情绪 happy/gentle/serious/surprise/neutral)/listening 短视频 + manifest.json + 匹配引擎（情绪事件/副文本驱动选片，无事件用音频能量兜底）；运行时零 GPU；MuseTalk/LatentSync 降级为可选增强 + 离线素材生成工具；素材来源三选一（云 GPU 一次性渲染 / 授权素材 / Live2D 预渲染）。
- DESIGN.md 已更新至 v0.2，PROJECT_MEMORY.md 同步。
- 待老板确认：素材来源方案。

## 素材策略定为"先占位后补"（同日追加）
- 老板选定素材来源：**先占位后补素材**。
- 开发期占位：① 开源授权样片循环验证画布+匹配+切换链路 ② 内置卡通形象+音频能量驱动嘴部。后补：云 GPU 渲染真实素材直接替换 assets/avatars/（manifest.json 不变，前端零改动）。
- 下一步：等老板一声令下，M1 用 init-cbc-sdk-web 搭脚手架。

## 老板加需求：Git 版本控制规范（同日追加）
- 老板要求设计里加 Git 版本控制：仓库初始化、.gitignore、提交信息规范、分支管理。
- DESIGN.md 升级 v0.3 新增 §14：git init/main 主干 + 首次提交 + Tag；.gitignore（依赖/构建/.env/数据库/大视频素材不入库，素材走 scripts/fetch-avatars.sh）；Conventional Commits（type+scope 中文 subject ≤50 字）；轻量 Git Flow（feature/fix/chore 分支 + --no-ff 合并 + 里程碑 Tag v0.1.0-m1~m5）。
- M1 第一步改为：git init + .gitignore 再脚手架。
- 待老板确认后开工。

## 功能评审：增删分析（同日追加）
- 老板要求从用户体验/实用性/技术可行性/维护成本四维度分析功能增删。
- 主要结论：建议新增长期记忆/人设档案、字幕显示、主动问候、语音回放、情绪波形；建议延后 5 个 Provider 只保 1+1、VSCODE/CODEX MCP（与 SDK 自身能力重叠）、MuseTalk sidecar 至 v2、PWA 延后。
- 已输出建议清单给老板，待拍板采纳哪些。

## v0.4 拍板：Qwen 端到端 + 功能收敛（同日追加）
- 老板拍板：**S2S 先做 Qwen 端到端**（阿里 DashScope，80ms 级、80+ 语言、OpenAI 兼容），其余厂商（GLM-Realtime 备选、Seeduplex/GPT/Gemini）写成预留接口不实现；其余按综合建议升级 v0.4。
- DESIGN.md v0.4 变更：新增 §15 功能决策记录——新增（v0.1）：长期记忆/人设档案（SQLite memories 表+摘要注入）、字幕显示（CaptionBar，S2S 副文本）、情绪波形（VoiceWaveform）；删除：WebRTC 视频推流（素材库用 <video> 直播）、Provider 收敛 5→1+预留、多 Agent 简化单一人设；延后：VSCODE/CODEX（v0.2，与 SDK 能力重叠）、MuseTalk sidecar（v2）、PWA（v1.5）、语音回放/主动问候/形象切换（v0.2+）。
- MCP v0.1 只接 Work Buddy + Hermes；里程碑改为 M1~M5 各打 Tag。
- 待老板确认后开工 M1。

## M1 施工中：脚手架 + 自动化测试整合（同日追加）
- 老板拍板"开工 M1"，并追加需求：**整合自动化测试**（范围覆盖核心模块/业务逻辑/边界；类型含单元+集成+E2E；自动命令+CI；报告含通过率/失败详情/覆盖率；可维护命名）。
- DESIGN.md 升级 v0.5：§11 扩展为完整自动化测试设计（Vitest 单元/集成 + Playwright E2E + GitHub Actions CI + aggregate-report.mjs 汇总 test-summary.md）。
- M1 实际进度：
  - ✅ git init + main 主干 + .gitignore（cybergirlfriend/.gitignore）
  - ✅ init-cbc-sdk-web 脚手架生成 cybergirlfriend/（React18+Vite5+TS+TDesign+Express4+SQLite+Agent SDK）
  - ✅ server/index.ts 改为 export app + 条件 listen（供集成测试）
  - ✅ 新增 server/mcp-servers.ts（v0.1 启用 workbuddy+hermes，vscode/codex 注释预留）
  - ✅ 新增 server/avatar/clip-matcher.ts（素材匹配核心纯函数，M3 预置）
  - ✅ 测试套件：tests/setup-env.ts、tests/unit/clip-matcher.test.ts（10 用例）、tests/integration/api.test.ts（6 用例）、tests/e2e/app.spec.ts（3 用例）
  - ✅ vitest.config.ts / playwright.config.ts / .github/workflows/ci.yml / scripts/aggregate-report.mjs / .env.example
  - ✅ package.json 加 test/test:unit/test:integration/test:e2e/test:coverage/test:all/report 脚本
  - ✅ README.md 重写：赛博女友标题 + 测试运行说明章节
  - ⏳ npm install 进行中（网络慢），完成后装 vitest/@vitest/coverage-v8/@playwright/test，跑测试验证，首次提交 + Tag v0.1.0-m1

## 依赖安装排障 + 国内源策略（同日追加，重要经验）
- **老板新要求**：依赖数量最小化 + 全部依赖可从国内源获取 + 提供依赖清单与国内下载配置。
- **本机 npm 挂起 Bug（重要）**：Node 22.22.2 的 npm 10.9.7 所有完整命令（install/view/config list）静默挂起（config get/--version 正常）；node fetch/spawn/fs 均正常 → 非网络问题，是 npm 自身问题；换 pnpm 也挂。
- **解药**：`NODE_DISABLE_COMPILE_CACHE=1`（Node 22 v8 编译缓存 bug）。pnpm 11.20.0 独立 tarball 已放 `.tools/pnpm/`，用 `node .tools/pnpm/package/bin/pnpm.mjs <cmd>` 运行。
- 产出：`.npmrc`（registry=npmmirror + better_sqlite3_binary_host 镜像 + shamefully-hoist）、`DEPENDENCIES.md`（依赖清单+国内源+排查）、DESIGN.md v0.6 §16（依赖最小化原则：纯 JS 优先、原生模块仅豁免 better-sqlite3、待瘦身项 @tdesign-react/aigc/less/图标二选一/uuid）。
- 状态：pnpm install 后台运行中。

## 依赖安装排障补充（同日）
- **WorkBuddy 环境大坑（重要）**：WorkBuddy 通过 `NODE_OPTIONS=--require=...genie-safe-delete.cjs` 给所有 node 进程注入"安全删除 shim"，会把 fs 删除重定向到回收站 → pnpm 清理临时目录报 [safe-delete] 错误。**解药：跑 pnpm 时清空 `NODE_OPTIONS=`**。
- pnpm store 索引 SQLite 曾被 kill 的进程弄成只读 → 用全新 store：`--store-dir="C:/Users/chipsine/AppData/Local/Temp/pnpm-store-fresh"`。
- 下载抖动：`--fetch-retries=8 --network-concurrency=4`。
- **pnpm 11 配置新家是 `pnpm-workspace.yaml`**（package.json 的 pnpm 字段不再读取）：已写 `onlyBuiltDependencies: [better-sqlite3, esbuild]`。
- 最终安装成功：node_modules 24 个顶层包（vite/agent-sdk/vitest/tsx 可用）。

## 老板暂停所有任务（同日）
- 老板要求暂停所有任务。
- 暂停时进度：依赖已装上（24 顶层包）；better-sqlite3 的 prebuild 二进制与 esbuild 构建脚本**尚未跑**（需重跑 `pnpm install` 触发，pnpm-workspace.yaml 已放行）；git 未首次提交。
- 恢复点：M1 从「重跑 pnpm install（触发构建脚本）→ 验证 better-sqlite3 → 跑测试 → git 首次提交 + Tag v0.1.0-m1」继续。
- 完整 pnpm 命令备忘：`NODE_OPTIONS= NODE_DISABLE_COMPILE_CACHE=1 node .tools/pnpm/package/bin/pnpm.mjs install --store-dir="C:/Users/chipsine/AppData/Local/Temp/pnpm-store-fresh" --fetch-retries=8 --network-concurrency=4`

## 老板要求重新考虑核心 AI 聊天 + 数据库架构（同日）
- 已读 db.ts 全文（better-sqlite3，227 行封装，sessions/messages 表）。
- **实测验证：node:sqlite（DatabaseSync）在 Node 22.22.2 可用**，API 与 better-sqlite3 几乎一致（prepare/run/all/exec），有 ExperimentalWarning。
- 分析要点（给老板的方案）：
  - AI 聊天：文本（Agent SDK 带工具/MCP）与语音（Qwen S2S 无工具）双链路割裂 → 建议统一 Chat Orchestrator 编排层（共享 Persona/Memory/SessionStore，双后端 ChatBackend 接口，工具类请求 v0.2 路由回文本链路）。
  - 数据库：better-sqlite3 原生依赖可移植性差 → 推荐换 **node:sqlite 零依赖**（已验证），db.ts 重写成本低；存储分层（DB 存结构化 + 文件系统存音频/视频）；schema 优化（messages 加 type/audio_path/subtitle、sessions 加 backend、schema_version 迁移）。
- 待老板拍板后更新 DESIGN v0.7。

## 架构变更评估完成：ST 式核心 + Hermes 工作执行（同日）
- 老板否决了 CodeBuddy Agent SDK 方案，要求评估 **SillyTavern 式轻量核心对话+角色卡 + Hermes agent 做工作执行** 的可行性。
- 调研了 SillyTavern 源码（chara_card_v2 角色卡格式、prompt 组装流程、LLM 后端代理）与 Hermes agent v0.20.0（CLI 命令、one-shot `hermes -z`、MCP serve 模式、50+ 内置工具）。
- 结论：**技术上完全可行**。DESIGN.md 升级 v0.7，新增 §17 完整评估（架构对比/角色卡字段/集成方式/依赖变化/风险缓解/实施计划）。
- 关键变化：去掉 @tencent-ai/agent-sdk + better-sqlite3，改为自研 Chat Core（~400 行 TS）+ node:sqlite 零依赖 + Hermes Runner（子进程 `hermes -z`）。
- 待老板确认三个问题：① Hermes 安装接受度 ② LLM 后端选哪个 ③ 是否 P0 先行（文字聊天先跑通）。
- 项目文件已更新：DESIGN.md v0.7、PROJECT_MEMORY.md 同步。

## 新需求：Qwen3-Omni 语音角色卡模式（同日）
- 老板新需求：类似 SillyTavern 的角色卡文件 → 发给 Qwen3-Omni → 发语音 → 实时返回语音+文字，不需要太高的智商（轻量即可）。
- 调研结论（DESIGN.md 升级 v0.8 新增 §18）：
  - **关键坑**：Qwen-Omni-Turbo 明确不支持 system message 角色设定（输出音频时自我认知仍是千问）；必须用 Qwen3-Omni-Flash（或 Qwen3.5-Omni）。
  - **角色卡本质 = system prompt 注入**：Qwen3-Omni 官方支持通过 system prompt 定制角色；chara_card_v2 → 组装 prompt 即可。
  - **SillyTavern 语音是 TTS 后处理非 S2S**，不直接适用；用 Qwen3-Omni 原生 S2S 更合适。
  - 开源项目对比：SGLang-Omni Playground（Realtime WS）、Qwen3-Omni-Simple-WebUI（FastAPI+DashScope 极简）、run-qwen3-omni（Node+Vite 多供应商+VAD）、gouzi（Rust 单文件+prompts 注入）、MiniCPM-o 4.5（9B 端侧备选）、OpenAvatarChat（数字人+Omni handler）。
  - 推荐路线 A：云端 DashScope `qwen3-omni-flash`（免 GPU、便宜、OpenAI 兼容、stream=True）；路线 B 本地 vLLM 需 40GB+ 显存。
- 待老板拍板：① 云端 vs 本地 ② 前端形态（现有项目加页面 vs 独立小应用）③ 角色卡来源 ④ 角色卡格式（复刻 chara_card_v2 vs 自定义）。
- 文件已更新：DESIGN.md v0.8（§18）、PROJECT_MEMORY.md。

## Qwen3-Omni 深度答疑 + 调研文档整理（同日）
- 老板连环追问：① 语音输入输出依据什么内容（不乱说的原理）② Thinker 思考器详解 ③ 角色卡怎么输入/多大/记忆多长/会不会覆盖 ④ 输入输出速度。
- 答疑要点（已用可视化讲解）：
  - 端到端原理：AuT 音频编码→语义 token→Thinker 推理→文本+隐藏状态→Talker 发声；不显式转文字但真听懂。
  - Thinker = 吃多模态 token 的 MoE 稀疏 LLM（30B 总参/3B 激活），逐字自回归生成，文本流=字幕、隐藏状态喂 Talker。
  - 角色卡：走 system prompt；**Qwen-Omni-Turbo 明确不支持角色设定，必须用 Qwen3-Omni-Flash-2025-12-01**（已全面开放 system prompt 自定义）；角色卡 2-3K tokens 只占窗口小部分。
  - 记忆：模型无状态，每轮重发全部上下文；flash 65K tokens 窗口（输入≤49,152/输出≤16,384），塞满挤掉最老的；需滑动窗口+摘要归档；assistant 消息只能纯文本、user 可带一种模态；音频 ≤100MB/20 分钟。
  - 速度：官方 211ms（音频）/507ms（视频）/234ms 冷启动；本地总体验 300-400ms、云端首包 0.5-1.5s；流式 80ms/帧边想边说，远不会 10s。
- 产出：`Qwen3-Omni-调研笔记.md`（项目根目录，8 章完整整理：概述/原理/Thinker/角色卡/记忆/延迟/部署对比/项目参考/落地建议）。
- 待老板拍板：云端 vs 本地、前端形态、角色卡来源与格式、是否先延迟实测。

## 老板转向 Qwen-Audio-3.0-Realtime-Flash（同日）
- 老板直觉：Qwen-Audio-3.0-Realtime-Flash 可能比 Qwen3-Omni 更适合 → 调研确认**老板判断正确**。
- 该模型 2026-07-15 发布（前身 Fun-Realtime-AudioChat），**专为实时语音对话**设计：时延 **<120ms**、**全双工可打断**（边说边听/抗噪/多人锁定/声纹聚焦 audio_prompt）、**情感化语音**（情绪/副语言/音色克隆/角色风格切换，官方场景含"情感陪伴"）、**instructions 字段直接支持角色设定**（官方示例即客服角色）、**原生 FunctionCall + MCP/API/知识库接入**、On-Policy Distillation（文本 LLM 推理蒸馏进语音模型）。
- 价格：Flash 入 ¥3/出 ¥30 每百万 token，Plus ¥5/¥40。
- 接入：WebSocket `wss://{WorkspaceId}.cn-beijing.maas.aliyuncs.com/api-ws/v1/realtime?model=qwen-audio-3.0-realtime-flash`，PCM16k 输入/24k 输出，conversation.item.create 插历史与系统指令，VAD server_vad/semantic_vad，enableInputAudioTranscription 可转文字。
- **结论：S2S 主选改为 Qwen-Audio-3.0-Realtime-Flash**（Qwen3-Omni 降为备选，因其开源可本地部署）。权衡：仅 API 数据过云。
- 文件更新：Qwen3-Omni-调研笔记.md 追加 §9 对比（发布/定位/时延/双工/情感/角色/工具/成本），PROJECT_MEMORY.md 同步。
- 待老板确认：主选 Flash？+ DASHSCOPE_API_KEY 实测（角色注入/打断/音色）

## Qwen-Audio-3.0-Realtime 选型调研报告（同日）
- 老板要求"先写一个报告"→ 产出 `Qwen-Audio-3.0-Realtime-调研报告.md`（项目根目录）。
- 报告结构：摘要（结论先行）/ 模型背景 / 核心能力（毫秒级响应、全双工、情感化语音、instructions 角色设定、Agent 工具调用、技术底子）/ Qwen3-Omni 对比表 / 成本与风险 / 选型建议（6 条理由）/ 下一步 / 附录接入速览。
- 结论：✅ 主选 Qwen-Audio-3.0-Realtime-Flash（<120ms、全双工可打断、情感陪伴定位、instructions 角色注入无坑、原生 FunctionCall+MCP）；Qwen3-Omni 降为备选（开源可本地化）。
- 待老板评审后：申请 API Key → 实测 → 接入方案设计。

## 豆包 Seeduplex 调研（同日）
- 老板要求调研豆包实时语音模型 3.0（Seeduplex）→ 产出 `豆包Seeduplex-调研报告.md`（项目根目录）。
- 关键事实：2026-06-18 火山引擎上线 API（**邀测制**），原生全双工端到端语音大模型；三大优势=精准遵循/抗干扰/动态判停（判停 -250ms、打断 -300ms、抢话 -40%）；全双工最彻底（语义级守听、多人主动加入）；原生 FunctionCall+MCP+自定义工具；WebRTC 接入；第三方定价 $0.008/分钟（待官方确认）。
- 火山豆包语音生态亮点：SystemMessages/HistoryLength 上下文管理、Prefill 降延迟、**IgnoreBracketText 情绪指令下发**（可驱动数字人表情，跟 DESIGN 素材匹配引擎契合）。
- 风险：邀测开通不确定、文档零散、定价未公开、情感陪伴弱于 Qwen、角色设定待实测。
- **三方对比结论：Qwen-Audio-3.0-Realtime-Flash 仍最稳主选；Seeduplex 并行申请邀测，达标则升级；Qwen3-Omni 本地化备选。**
- 文件更新：豆包Seeduplex-调研报告.md、PROJECT_MEMORY.md。

## 老板拍板核心架构：云端语音壳 + 本地大脑（同日）
- 老板核心思想：Qwen-Audio-3.0-Realtime-Flash 加载简单任务背景+产品知识做快问快答；复杂问题→文本给本地模型→处理→文本反馈→Qwen-Audio"说出来"。
- 核实关键 API（阿里云官方文档）：`conversation.item.create`（role:system/user/assistant + input_text 文本注入）、Function Calling（function_call → function_call_output → response.create）、`enableInputAudioTranscription`（qwen3-asr-flash-realtime 用户语音转写）、`session.update` instructions（角色/知识注入）、turn_detection 三种模式（server_vad/smart_turn/push-to-talk）、modalities 双输出。
- 产出：`混合架构方案-云端语音壳+本地大脑.md`（两路径：A Function Calling 中转推荐 / B 手动文本注入；延迟估算：简单<1s、复杂 1.5-5s；本地模型选型建议 Ollama+Qwen3-8B CPU 起步）。
- 待老板确认：路径 A/B、本地模型选型、产品知识内容、字幕存库。
- 文件更新：混合架构方案-云端语音壳+本地大脑.md、PROJECT_MEMORY.md。

## 老板更新核心思想：Hermes 大脑（同日 v2）
- 老板把"本地模型"具体化为 **Hermes**：Qwen-Audio 加载**简单人物背景**（人设快问快答），复杂问题文本转 Hermes，Hermes 处理后文本回传让 Qwen-Audio 说出来。
- **实测确认 Hermes v0.20.0 已在本机安装**：C:\Users\chipsine\AppData\Local\hermes\hermes-agent，Python 3.13.14，OpenAI SDK 2.24.0，`hermes -z` 可用。
- 混合架构方案升级 v2：云端 Qwen-Audio 语音壳 + Hermes 大脑（人设注入 instructions；Hermes 注册为 function 工具走 Function Calling 或手动文本注入；hermes-runner.ts 子进程调用见 DESIGN §17）。
- 待老板拍板：路径 A/B、Hermes 后端模型（DeepSeek/OpenAI/Ollama）、人物背景（小呆人设）、判定规则、字幕存库。
- 文件更新：混合架构方案-云端语音壳+本地大脑.md（v2）、PROJECT_MEMORY.md。

## 核心架构设计任务完成（同日）
- 老板指派核心架构设计任务（专家：SoftwareArchitect）：梳理现有架构 + 产出整体架构图与设计思路 + 按任务模块建目录。
- 已读全部资料：混合架构方案 v2（老板定稿）、DESIGN.md v0.7（含 §17 评估）、三份 S2S 调研（Qwen-Audio-3.0-Realtime / Qwen3-Omni / 豆包 Seeduplex）、PROJECT_MEMORY.md、cybergirlfriend 脚手架结构。
- 产出 `docs/architecture/overall-architecture.md`（架构总纲 v1.0）：
  - 架构梳理：决策脉络（v0.1→定稿）、职责边界表、6 条关键约束、6 个重构点（去 SDK、换 node:sqlite、废弃 mcp-servers、收敛单一人设等）
  - 整体设计：模块划分原则（可插拔供应商 voice-shell/brain + 产品能力 persona/avatar/memory + 支撑底座 data/app）、三条核心流程（快问快答 <1s / 复杂任务 1.5-6s / 长期记忆）、数据流向、扩展性矩阵（换供应商/加模块/本地化/多角色）
  - 落地红线：文本中转不漂移、语音壳不碰业务、Hermes 无状态、数据库零原生依赖、AI 输出可审计
- 按任务模块建目录（根目录）：voice-shell / brain / persona / avatar / memory / data / app（含 server/）/ client + docs（architecture/research/adr）/ assets / scripts / tests，每个模块写了 README.md（职责/核心文件/约束/接口/相关文档）。
- 调研报告归档：三份调研文档移入 docs/research/，根目录只留总纲（DESIGN.md、PROJECT_MEMORY.md、混合架构方案）。
- PROJECT_MEMORY.md 已同步。

## 🚫 环境搭建永久暂停（同日，老板明确指示）
- 老板原话："环境不用搭建了，不是你负责的事情，本任务永久暂停环境搭建"。
- 明确边界：不再执行 npm/pnpm install、Python 依赖安装、素材下载（fetch-avatars.sh）、工具链配置等任何环境类操作；DESIGN §16 / DEPENDENCIES.md 仅作历史记录不再维护。
- 环境由老板自己/其他渠道负责；本任务只做**架构设计与代码实现**。
- 已写入 PROJECT_MEMORY.md（项目概述 + 下一步计划）固化此决策。

## 老板确认角色：总体架构负责人（同日）
- 老板明确：我是**总体架构负责人**——把握整体架构和方向，给各功能模块安排任务、看执行情况、做汇总。
- 顺手收口：把旧的"测试框架与 CI 工作流"任务（#4）正式封存为 completed（已暂停，不再推进），任务列表 10/10 全部关闭。
- 整体架构图已可视化呈现（浏览器 client → Core Orchestrator → 5 能力模块 voice-shell/brain/persona/avatar/memory → 支撑 data/assets → 外部 Qwen-Audio 云端 + Hermes 本机）。

## 架构负责人继续推进：ADR + 模块契约（同日）
- 产出 `docs/adr/README.md`：6 条 ADR 固化决策（001 混合架构、002 弃用 SDK 自研 Core、003 node:sqlite、004 素材库数字人、005 测试/CI 暂停、006 环境搭建永久暂停）。
- 产出 `docs/architecture/module-contracts.md`（模块契约 v1.0）：各模块接口定义（VoiceProvider / BrainRunner / PersonaBuilder / ClipMatcher / MemoryStore / Db）、WS 协议（/ws/voice 事件）、数据模型 schema、5 条开发约束（只依赖接口、文本中转不漂移、错误统一、类型共享、新增接口先改契约）。
- 后续任务开工可直接按契约开发，互不冲突。

## 🔧 纯交互界面收敛：删除记忆系统与数据库（同日，老板明确）
- 老板明确："关于记忆系统和数据库这些删除掉，有 Hermes 负责处理。赛博女友项目只是个交互界面，简单的问答，具体的事情有 Hermes 负责。"
- **执行**：
  - 删除 `memory/`、`data/` 目录（含 README）
  - 架构总纲升级 v1.1：模块收敛为 4 能力（voice-shell/brain/persona/avatar）+ 1 支撑（app）+ client；关键约束新增"无本地状态、零持久化、无状态服务"
  - 模块契约升级 v1.1：删除 MemoryStore / Db 接口，新增"零持久化"开发红线
  - ADR：新增 ADR-006（纯交互界面，事务与记忆归 Hermes）；ADR-003（node:sqlite）标记作废
  - brain README 明确"记忆归 Hermes"；persona README 去掉上下文管理
  - PROJECT_MEMORY.md 同步（简短描述、M1 计划去掉数据库切换项）
- 影响：赛博女友侧零持久化，记忆/会话状态由 Hermes 自带记忆系统负责。

## ADR-005 删除（同日）
- 老板要求删除 ADR-005（测试框架与 CI 暂停）条目，已删除并把原 ADR-006（环境搭建暂停）顺位为 ADR-005。

## 架构优化：persona 归 Hermes + APIKEY 集中配置 + 轻量化（同日）
- 老板三项需求：① persona 人设由 Hermes 统一维护，赛博女友仅保留接口定义 ② 新增 APIKEY 配置文件集中管理 ③ 审查支撑层和前端代码，轻量级配置、减少依赖和代码体积。
- 审查旧脚手架全部源码：24 文件 ~5316 行，13 运行时依赖。
- 产出 `docs/architecture/optimization-report.md`（完整优化报告）。
- **persona 重构**：PersonaBuilder → PersonaProvider 抽象接口（listPersonas/getPersona/buildInstructions/switchPersona），人设数据归 Hermes，删除本地角色卡文件。
- **APIKEY 配置**：新增 `config/` 目录（apikeys.example.json + loader.ts），文件优先、环境变量兜底。
- **轻量化方案**：运行时依赖 13→5-6 个（删 SDK/DB/router/TDesign/uuid），前端组件 10 个 3224 行→5 个 ~310 行，总代码 ~5316→~1003 行（-81%）。
- module-contracts.md 升级 v1.2（PersonaProvider + 配置集中管理约束）。
- 新增 ADR-007（人设归 Hermes + APIKEY 集中 + 轻量化）。
- TASKS.md 更新：persona 任务重构（PS-01~03 改为接口/Provider/切换API），新增 config 任务 CF-01（已完成）/CF-02。
- BLUEPRINT.md 更新：红线扩至 10 条，persona 模块描述更新。

## 三文档工作流管理系统建立（同日）
- 老板要求建立"项目协作机制的基础设施"——三文档工作流管理，让架构能自我解释、子模块能自主执行。
- 产出四份文档：
  - `docs/BLUEPRINT.md`（项目蓝图）：一站式入口——项目愿景/架构速览/模块清单/红线/技术栈/文档索引/快速上手/里程碑概览
  - `docs/TASKS.md`（任务看板）：M0~M5 全模块任务清单——每条含 ID/优先级/状态/依赖/验收标准，附任务依赖关系图
  - `docs/DEVLOG.md`（开发日志）：按时间倒序模板 + 历史工作回填（架构定稿阶段全部决策与产出）
  - `docs/WORKFLOW.md`（工作流规则）：接任务→读文档→干活→写日志→更看板完整流程，含文档维护/任务管理/架构变更/检查清单规则
- 更新 `docs/README.md` 索引，三文档列为核心入口
- 任务 ID 规则：VS/BR/PS/AV/AP/CL/DC/M{X} 前缀 + 序号
- 状态标记：📋TODO / 🔄IN PROGRESS / ✅DONE / 🚫BLOCKED / ⏸PAUSED
- 优先级：P0阻塞 / P1核心 / P2增强 / P3延后

## 素材匹配引擎讲解 + 模块优先级排名 + Git 初始化（同日）
- 老板问素材匹配引擎（clip-matcher）是干什么的 → 用两张可视化图讲解（语音链路位置图 + 选片决策流程图）+ 权衡分析（零GPU vs 拼接感/MuseTalk 预留）
- 老板确认方案 ✅，要求：对所有功能模块做优先级排名 → 更新子任务 → 更新各文档 → 更新 Git
- **模块优先级排名**（写入 TASKS.md 顶部）：config(1) → app(2) → persona(3) → brain(4) → voice-shell(5) → avatar(6) → client(7) → docs(8)；依据=依赖拓扑 + 核心体验贡献 + 风险先行
- 更新 TASKS.md v1.1：新增优先级排名章节、修正依赖关系图（PS-01/PS-02 改 PersonaProvider 版）、AV-01/AV-02 标注方案已确认
- 更新 BLUEPRINT.md v1.1：模块清单加排名、技术栈 TDesign→Tailwind、M3 标注方案已确认
- DEVLOG.md 追加"模块优先级排名 + 素材匹配方案确认 + Git 初始化"记录
- **Git 初始化**（CF-02 完成）：
  - `.gitignore` 新建：config/apikeys.json、node_modules、assets/avatars/、cybergirlfriend/_tmp_*、cybergirlfriend/.tools/ 等忽略
  - 排障：cybergirlfriend/ 有嵌套空 .git 仓库导致 add 失败 → 删除嵌套 .git（无 commit 无损失）→ 正常 add
  - 首次提交 `49252a5`：81 文件 +18693 行，main 分支，Conventional Commits

## GitHub 开源上线（同日，重要排障经验）
- 老板要求更新到 GitHub 并设置开源模式 → 创建 `shilijiana/cyber-girlfriend`（Public）仓库
- **排障链（重要经验）**：
  1. MCP GitHub 连接器只读（create_repository / create_or_update_file 均 403 "Resource not accessible by integration"）→ 连接器权限由 WorkBuddy 预设，改不了
  2. 老板提供 fine-grained PAT（github_pat_ 开头）→ curl 认证通过但写文件仍 403 "Resource not accessible by personal access token"（token 缺 Contents 写权限）
  3. git push 在 Git Bash 后台环境无 tty，GCM 弹窗弹不出来
  4. **最终解药：SSH key 方案**——本机已有 `~/.ssh/id_ed25519`，让老板把公钥加到 GitHub（https://github.com/settings/keys）→ `ssh -T git@github.com` 验证成功 → remote 切 `git@github.com:shilijiana/cyber-girlfriend.git` → push 成功
- **经验**：GitHub 推送受阻时，优先检查本机 SSH key + 添加公钥，这是最干净的一劳永逸方案（不依赖连接器权限/token）
- 推送成功：main 分支 81 文件，提交 c9dfe70（本地与远程一致）
- 密钥安全验证：远程 config/ 只有 apikeys.example.json + loader.ts，apikeys.json（含真实 token）未入库 ✅
- 老板的 PAT 已存 config/apikeys.json（gitignore 忽略），但已不需要（SSH 生效），后续可作废该 token
