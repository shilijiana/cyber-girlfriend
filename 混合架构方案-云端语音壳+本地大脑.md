# 混合架构方案：Qwen-Audio 语音壳 + Hermes 大脑

> **文档日期**：2026-08-09（v2，老板更新核心思想）
> **项目**：赛博女友（Cyber Girlfriend）—— AI 数字人语音陪伴应用
> **核心思想（老板 2026-08-09 定稿）**：
> Qwen-Audio-3.0-Realtime-Flash 加载**简单人物背景**，实现和老板的**快问快答**；
> 复杂问题：Qwen-Audio 输入文本给 **Hermes** → Hermes 处理 → 文本反馈给 Qwen-Audio → 让 Qwen-Audio "说出来"。

---

## 1. 方案定位（一句话）

**云端 Qwen-Audio-3.0-Realtime-Flash 当"嘴和耳朵"（语音交互 + 人设快问快答），Hermes agent 当"大脑"（复杂逻辑/干活），中间用文本衔接。**

- 简单对话 → Qwen-Audio 直接答（毫秒级，不打扰 Hermes，人设由 instructions 注入）
- 复杂问题 → 文本转 Hermes → Hermes 处理（可调用 50+ 工具干活）→ 结果文本回传 → Qwen-Audio 用语音 + 字幕"说出来"

## 2. 为什么这个方案聪明

| 好处 | 说明 |
|---|---|
| **语音体验有保障** | Qwen-Audio <120ms、全双工可打断，语音质感/交互节奏全保住 |
| **人设/对话在云端壳** | 简单人物背景（性格/语气/称呼）注入 instructions，快问快答不烧脑 |
| **复杂逻辑给 Hermes** | Hermes 不只是 LLM，是**能干活的 agent**（终端/文件/浏览器/Git 50+ 工具）——"女友帮忙办事"场景天然支持 |
| **成本最优** | 简单对话不烧云 token；复杂任务走本地 Hermes（免费） |
| **解耦灵活** | Hermes 可配任意后端（200+ 模型 33 provider），Qwen-Audio 不动 |
| **已具备落地条件** | ✅ Hermes v0.20.0 已在本机安装（Python 3.13.14），`hermes -z` 立即可用 |

## 3. 关键 API 支撑（已核实 ✅）

Qwen-Audio-3.0-Realtime 原生支持文本中转所需的一切：

| 能力 | API | 用途 |
|---|---|---|
| **注入文本消息** | `conversation.item.create`，`role: user/assistant/system`，`type: input_text` | 把 Hermes 的文本结果喂回，让 Qwen-Audio 朗读 |
| **Function Calling** | 模型生成 `function_call` → 客户端执行 → `function_call_output` 写回 → `response.create` | 把 Hermes 注册成工具，Qwen-Audio 自动判断何时调用 |
| **用户语音转写** | `enableInputAudioTranscription`（qwen3-asr-flash-realtime）+ `transcript` 事件 | 拿到老板说的文本，发给 Hermes |
| **角色/人设设定** | `session.update` 的 `instructions` 参数 | 注入简单人物背景（名字/性格/语气/关系） |
| **语音+文字双输出** | `modalities: ["text","audio"]` | Hermes 的回答被朗读的同时显示字幕 |
| **交互模式** | `turn_detection.type`: `server_vad` / `smart_turn` / `push-to-talk` | 快问快答用自动 VAD 或手动按键 |

## 4. Hermes 大脑（已装 ✅，能力复核）

**Hermes Agent v0.20.0（2026-08-03）**，本机状态：
- 路径：`C:\Users\chipsine\AppData\Local\hermes\hermes-agent`
- Python：3.13.14，OpenAI SDK 2.24.0

| 能力 | 说明 |
|---|---|
| **one-shot 调用** | `hermes -z "任务描述"` → stdout 返回纯文本结果，适合管道捕获 |
| **50+ 内置工具** | 终端执行（6 种后端）、文件操作、浏览器自动化、Git 操作 |
| **MCP 支持** | `hermes mcp serve`（10 个 MCP 工具：对话管理/消息收发/审批），也可连外部 MCP |
| **模型后端** | 200+ 模型、33 个 provider（OpenAI/Anthropic/DeepSeek/Ollama 等） |
| **自进化** | 从任务中学习，沉淀为可复用技能 |

> 详见 DESIGN §17.3（Hermes 工作执行能力调研）与 §17.5（集成方式：`hermes-runner.ts` 子进程调用）。

## 5. 两种实现路径

### 路径 A：Function Calling 中转（推荐，符合模型原生设计）

把 Hermes 注册为 Qwen-Audio 的一个"工具"，模型自己判断"这个该我答还是该找 Hermes"：

```
老板语音 → Qwen-Audio 听懂
   ├─ 简单问题 → 模型自己答（人设快问快答）
   └─ 复杂问题 → 模型发出 function_call("hermes_brain", {task})
                   ↓
        客户端收到 → hermes -z "任务"（子进程，2 分钟超时）
                   ↓
        function_call_output 写回 → response.create
                   ↓
        Qwen-Audio 用语音+字幕"说"出 Hermes 的结果
```

**优点**：意图分流由模型自动做；完全复用官方 Function Calling 机制，稳定。
**注意**：工具 schema 要小而严格（只传"任务描述 + 上下文摘要"）。

### 路径 B：手动文本注入（更直接，控制力强）

```
老板语音 → Qwen-Audio 转写（transcript 事件）→ 拿到文本
   ↓
（我们自己判断：简单 or 复杂）
   ↓ 复杂
hermes -z "任务" → 得到结果文本
   ↓
conversation.item.create 注入 assistant 文本消息 → response.create
   ↓
Qwen-Audio 朗读 + 字幕
```

**优点**：所有分流逻辑自己掌控，Hermes 执行形式灵活（one-shot / MCP）。
**缺点**：要自己管理对话状态（哪条消息对应哪轮）。

> **建议**：先走路径 A（Function Calling），它是 Qwen-Audio 官方为"语音 Agent"设计的正规用法；若意图分流不准，退到路径 B 手动控制。Hermes 调用方式按 DESIGN §17 用子进程 `hermes -z`（120s 超时）。

## 6. 数据流与延迟估算

| 环节 | 耗时（估） |
|---|---|
| 老板说完 → Qwen-Audio 转写 | ~200-400ms |
| 文本传给 Hermes | ~0ms（本机） |
| Hermes 处理（简单任务） | 1-5s（视任务复杂度与后端模型） |
| 结果文本写回 → Qwen-Audio 首包语音 | ~200-500ms |
| **总（复杂问题）** | **约 1.5-6s** |
| 总（简单问题，Qwen-Audio 直接答） | **<1s** |

> 快问快答体感没问题；复杂任务会有"思考一下"的停顿，可加过渡语"稍等，我去看看…"（路径 A 可在工具调用前让模型先说处理中）。

## 7. 系统架构（结合项目现状 + DESIGN §17）

```
浏览器（麦克风 + 播放 + 字幕 + 数字人画布）
   │ WebSocket
   ▼
Express 后端（自研 Chat Core，延续 DESIGN §17）
   ├─ voice-gateway.ts       # /ws/voice 中继，双路分发（播放/字幕/数字人）
   ├─ qwen-audio-client.ts   # Qwen-Audio Realtime WS 客户端（instructions 注入人设）
   ├─ function-router.ts     # Function Calling 中转：Hermes 注册为工具
   ├─ hermes-runner.ts       # hermes -z 子进程调用（DESIGN §17 已有设计）
   └─ prompt-builder.ts      # 简单人物背景 → instructions；上下文管理
   │
   ▼
Qwen-Audio-3.0-Realtime-Flash（云端，语音壳）
   │ 文本中转（function_call / conversation.item.create）
   ▼
Hermes Agent v0.20.0（本机，大脑）
   ├─ hermes -z "任务"（子进程 one-shot）
   └─ hermes mcp serve（可选，常驻多轮）
```

## 8. 待老板确认

1. **路径选择**：Function Calling 中转（A，推荐）还是手动文本注入（B）？
2. **Hermes 后端模型**：Hermes 调 200+ 模型，老板想用哪个？（DeepSeek / OpenAI / 本地 Ollama？）
3. **人物背景内容**：简单人物背景（名字/性格/语气/与老板关系）需要老板提供或小呆起草"小呆"人设？
4. **哪些问题走 Hermes**：路径 A 靠模型自动判断；是否需要自定义规则（如"查询/计算/操作类必须走 Hermes"）？
5. **字幕/日志**：Hermes 返回的文本是否存库（SQLite messages），便于复盘？

---

*方案 v2 · 2026-08-09 · 小呆 · 核心思想已定稿（Qwen-Audio 语音壳 + Hermes 大脑）*
